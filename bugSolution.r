```r
# Define the function or load the necessary package
my_nonexistent_function <- function(x) {
  x^2
}

#Now the function call will work
result <- my_nonexistent_function(10)
print(result) # Output: 100
```